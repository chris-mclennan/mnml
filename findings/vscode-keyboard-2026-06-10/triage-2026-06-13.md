# Triage — 2026-06-13

Driven through the file-IPC headless harness against the post-Session-3
build of mnml (commit `a228e4b` series + Session-B SEV-2 fixes).

## S1-01 — Ctrl+W close-pty: **not reproducible**

Setup:

```sh
mkdir -p /tmp/mnml-c-test && cd /tmp/mnml-c-test
echo 'fn main() {}' > main.rs
echo '' > readme.txt
MNML_COLS=160 MNML_ROWS=50 mnml /tmp/mnml-c-test --input standard --headless &
```

IPC commands fed:

```jsonl
{"cmd":"open","path":"main.rs"}
{"cmd":"run-command","id":"view.split_right"}
{"cmd":"open","path":"readme.txt"}
{"cmd":"run-command","id":"term.shell"}
```

State after spawn — 4 panes (`main.rs`, `main.rs` (split twin),
`readme.txt`, `terminal (zsh)`), `activePane = 3` (the pty), focus =
`pane`.

### Ctrl+W on the focused pty

```jsonl
{"cmd":"key","key":"ctrl+w"}
```

State after: 3 panes (`main.rs`, `main.rs`, `readme.txt`), `activePane =
0`. The pty closed. The split twin did **not** also close.

### `view.close_split` on a focused pty

After respawning the shell:

```jsonl
{"cmd":"run-command","id":"term.shell"}        # back to 4 panes, active=3
{"cmd":"run-command","id":"view.close_split"}  # pty focused
```

State after: 3 panes, `activePane = 0`. The pty closed.
`events.jsonl` shows `{"event":"command_run","id":"view.close_split","ok":"true"}`
**and** the pane count drops — the prior tester complaint ("ok:true but
pane stays") doesn't reproduce either.

### Conclusion

Both pieces of S1-01 (no chord closes a focused pty pane;
`view.close_split` returns success without closing) pass against
current code. Whatever shipped between the 2026-06-10 hunt and today
fixed it. No further action required this pass; reopen if the bug
reappears on a layout shape I haven't covered.

## S2-12 — same root as S1-01

Marked closed-by-S1-01.

## Other SEV-2s (not retried this pass)

- S2-01 / S2-03 / S2-04 — already-shipped chord bindings (see Session-3
  commit `a228e4b` for the new ones; the rest were already there).
- S2-02 Ctrl+L, S2-09 tree Enter spawning terminal, S2-10 Esc → tree —
  deferred; need targeted investigation under specific layout state.
