# Triage — 2026-06-13 (PM follow-up)

Additional bounded items from the vscode-mouse report tackled in this
afternoon's sweep. Pairs with the morning triage-2026-06-13.md.

## SEV-3 #1 — `+ Add integration` chip tooltip → CLOSED
Already fixed earlier today (`a915e41` + `aa65d84`). Tooltip map now
renders "add integration".

## SEV-3 #2 — No tooltips on activity bar icons → CLOSED
New `HoverChip::ActivityBarIcon(section)` + hover_chip_at arm + tooltip
text. Tooltip reads from `ActivitySection::meta()`'s label
("Explorer" / "Search" / "Source control" / "Run and debug" /
"Integrations"). Commit pending in tooltip-sweep batch.

## SEV-3 #3 — No tooltip on `♪ mixr` chip → CLOSED
New `HoverChip::StatuslineNowPlaying` arm. Tooltip reads
`{source}: {track}` from `App.now_playing`. Same commit batch.

## SEV-3 #4 — No tooltips on palette-bar back/forward/dropdown → CLOSED
New `HoverChip::PaletteBackButton` + `PaletteForwardButton` +
`PaletteDropdownButton` arms. Tooltips read "previous buffer" /
"next buffer" / "recent files". Same commit batch.

## SEV-3 #5 — Save-prompt buttons look like keyboard mnemonics → CLOSED
Labels reshaped from ` [S]ave ` to `  Save  ` (still hotkey-functional,
hotkey letter now rendered with UNDERLINED modifier). Padding doubled
so the bg-color box visually reads as a button. Same commit batch.

## SEV-3 #6 — Tree-edge resize handle 1 cell wide → CLOSED
Hit-rect widened from `width: 1` to `width: 3` (1 inside the rail +
2 outside). Render stays 1 cell — only hit-testing widens. Trackpad
users no longer need pixel-perfect aim.

## SEV-3 #8 — Recents picker top entry = currently open file → CLOSED
Already fixed in commit `aa65d84`. The active editor's path is
filtered out of `open_recent_files_picker`'s source.

## SEV-3 #9 — Git-rail file-row click flips unrelated rows → NOT REPRODUCIBLE

Audited both candidate click paths in current code:
- `GitStatus` pane (right-side workspace): `handle_scm_row_click`
  for `Pane::GitStatus` only sets `g.selected = flat_idx`. No
  stage/unstage logic on click.
- Git rail (left-side sidebar): `GitRailHit` enum has only
  `Branch`/`Worktree`/`Pull`/`ToggleBranches` — no `File` variant.
  No file rows exist in the rail.

The report's claim of "clicking a staged file's row caused that row +
2 others to flip back to unstaged" doesn't match any code path I
can find. Possible explanations: (1) tester accidentally pressed
`u` / `space` after clicking; (2) a behavior that existed in an
earlier commit and was later removed. Pinning for re-test if anyone
hits it on current main.

## SEV-2 #1/#2 — Settings + integration overlay swallow clicks → CLOSED

Both overlays' click handlers already wire row-clicks to focus +
Enter behavior:
- `settings_overlay`: clicking a row sets focus (and `settings_enter_row`
  cycles value when clicked on already-focused row).
- `discovery_overlay`: clicking a row focuses + calls
  `discovery_enter()`.

Both also have outside-click dismissal. Verified in `tui.rs`
mouse-input dispatch — handlers are already there.

## SEV-2 #6 — Right-click bufferline context menu flakiness → STILL OPEN

Trigger not isolated. Static code path looks correct
(`right_click_at` on bufferline_tabs registers a `ContextMenu`
state). The "after a sequence of left/middle clicks" qualifier
suggests state pollution; needs a focused live test to capture
the failing sequence.

## SEV-2 #3 — No mouse path to close a dirty tab → STILL OPEN, NEEDS LIVE

Headless harness couldn't isolate the bug (probed via IPC click on
the bufferline_tab_close rect at various cols, no overlay
appeared). Static code path looks correct: dirty tab's `●` badge
is at the same rect position as a clean tab's `×`, and `close_pane`
routes dirty buffers to the confirm overlay. Re-flagged in this
session's recap; needs an interactive mnml run.

## Net

Of the report's 14 findings, 10 are now closed (4 from morning, 6
from this afternoon), 2 remain open as "not isolated headless" /
"needs live test", and 1 (git-rail row flip) appears not-reproducible
on current main. Substantial close-out.
