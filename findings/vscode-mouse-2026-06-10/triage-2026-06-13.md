# Triage — 2026-06-13

Reviewed against current code; did not drive headless mouse repros
(harness coverage for click semantics inside chrome is gappy — see
`hybrid-2026-06-10` for the tmnl harness). For each item below: a
status verdict + a fix pointer if obvious.

## SEV-2

1. **Settings overlay swallows clicks on option values** — open.
   Per CLAUDE.md the settings convention is keyboard arrows for
   discrete-choice rows. The visual cue is the problem; the fix
   is either to make the chips clickable (cycle on click) or to
   stop rendering them as `[on] / off` chips that look clickable.
   Bounded fix worth a future session.

2. **`+ Add integration` overlay swallows clicks** — open. Same
   shape as #1; the keyboard-only flow is intentional but the
   visual is misleading. Fix is to wire click-on-row → focus +
   click-on-action-glyph → run.

3. **No mouse path to close a dirty tab** — open. The close-X
   swap to `●` was deliberate (visual dirty indicator) but the
   `●` should accept a click + open the save-prompt instead of
   being inert.

4. **Double-click no select word, triple-click no select line** —
   open. `tui.rs` tracks `last_click` for SCM panes; the editor
   mouse-down branch needs the same wiring. Small bounded fix.

5. **Tree single-click is preview; double-click pin missing** —
   open. The doc claim is in `tree_view.rs`. Wire the existing
   `app.last_click` count into the tree click handler.

6. **Right-click on bufferline tab sometimes fails to open menu**
   — open, NOT REPRODUCED reliably. Trigger not isolated. Pin
   for re-investigation when a tester can isolate the sequence.

## SEV-3

1. `+ Add integration` chip tooltip shows raw id — open. Wire
   a tooltip string in the chip's `HoverChip` registration.
2. No hover tooltips on activity bar icons — open.
3. No hover tooltip on `♪ mixr` chip — open.
4. No hover tooltips on palette-bar back/forward/dropdown — open.
5. Save-prompt buttons look like keyboard mnemonics — open, UX
   question.
6. Tree-edge resize handle 1-cell wide — open, polish.
7. Vertical editor scrollbar invisible — open. SEV-3 from earlier
   hunts also flagged this.
8. Recent-files top entry = current open file — open, easy fix
   (filter the active file out of the picker source).
9. Git-rail file-row click flipping unrelated rows — open. Needs
   a focused investigation; this is the only "buggy semantic"
   item in the SEV-3 list, others are missing-tooltip / polish.

## Net

Of 14 findings, 0 closed-since-report, 13 open, 1 not-reproducible.
This finding set hasn't been touched since the report; the chrome
work this week (chord chains, welcome dismissal, Esc behaviour)
didn't touch mouse semantics. A dedicated mouse-polish pass would
close most of these in a single session.
