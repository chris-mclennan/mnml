---
title: Changelog
description: Release notes + subscribe-to-updates for mnml.
---

import { LinkButton } from '@astrojs/starlight/components';

## Subscribe to releases

Get an email when a new version of mnml ships — no spam, just release notes.


<div style="margin: 1.5rem 0 2rem; padding: 1.25rem; border: 1px solid var(--sl-color-gray-5); border-radius: 0.5rem;">
<form
  action="https://buttondown.com/api/emails/embed-subscribe/chrismclennan"
  method="post"
  target="popupwindow"
  onSubmit="window.open('https://buttondown.com/chrismclennan', 'popupwindow')"
  style="display: flex; gap: 0.5rem; align-items: center; flex-wrap: wrap;"
>
  <input
    type="email"
    name="email"
    placeholder="you@example.com"
    required
    style="flex: 1; min-width: 220px; padding: 0.5rem 0.75rem; border: 1px solid var(--sl-color-gray-4); border-radius: 0.375rem; font-size: 1rem;"
  />
  <input
    type="submit"
    value="Subscribe"
    style="padding: 0.5rem 1rem; border: none; border-radius: 0.375rem; background: var(--sl-color-accent); color: var(--sl-color-accent-low); cursor: pointer; font-weight: 600;"
  />
</form>
</div>

Or watch the repo on GitHub for a notification on every release:

<LinkButton href="https://github.com/chris-mclennan/mnml/releases" variant="secondary">
  View all releases on GitHub
</LinkButton>

Already subscribed? [Manage your subscription](https://buttondown.com/chrismclennan/portal) (change email, unsubscribe, etc.).

---

## Recent releases

Versions auto-tagged at [github.com/chris-mclennan/mnml/releases](https://github.com/chris-mclennan/mnml/releases) — this page summarizes the user-facing changes. For commit-level history, see the GitHub release notes.

### Unreleased

User-facing changes landed on `main` since v0.1.2 and waiting for the next tag:

- **Startup picker** — JetBrains-style chooser overlay shown on launch when invoked with `--startup-picker` (or `MNML_STARTUP_PICKER=1`). Rows: New file · Open file… · 1-9 entries from `[[workspaces]]`. Keys: ↑↓ / jk move · Enter / 1-9 select · Esc / q skip. The `mnml.app` and `mnml-nightly.app` launchers now export the env var so opening mnml from Finder lands on the chooser instead of dropping you into `$HOME`. See the [Startup picker manual](/manual/startup-picker/).
- **Update-check toast** — on launch, mnml pings `api.github.com/repos/chris-mclennan/mnml/releases/latest` in a background thread and shows a one-shot toast with the release URL if a newer tag is available. Opt out with `[ui] check_updates = false`. Skipped automatically in `--headless` mode. See the [Update check](/manual/settings/#update-check) section in Settings.
- **Nightly app bundle** — `./scripts/build-app.sh --nightly` builds `target/mnml-nightly.app` with an inverted-color icon and a launcher that execs your latest `cargo build --release` directly. Coexists with the stable bundle in `/Applications/`. Local-only; not part of release CI. See the [Troubleshooting page](/troubleshooting/#nightly-bundle).
- **macOS Tahoe fix** — `Info.plist` now sets `LSMinimumSystemVersion = 11.0`, which suppresses the spurious "Support Ending for Intel-based Apps" warning on macOS 26 (Tahoe) for arm64-native builds. If you see that warning, redownload the latest DMG. See [Troubleshooting](/troubleshooting/#intel-based-apps-warning-on-macos-tahoe-26).
- **Full-bleed app icon + auto-version-stamped CFBundleVersion** — the icon now paints edge-to-edge so Tahoe's auto-glass-template wraps the art directly instead of leaving a gray bezel; `CFBundleVersion` is stamped with the build timestamp at package time so Finder picks up new artwork without a manual icon-cache flush.
- **Docs:** new manual page for the [Startup picker](/manual/startup-picker/), new [Jira tickets viewer](/manual/integrations/tickets-jira/) integration page, new [Troubleshooting](/troubleshooting/) page, and an [Update check](/manual/settings/#update-check) subsection in Settings.

### 0.1.2 — 2026-05-31

- macOS `.dmg` artifact now ships with cargo-dist's standard naming (`mnml-rs-<triple>.dmg`)
- Install page's macOS download button points at the DMG (drag-to-install)
- Smaller fixes (release pipeline cleanup)

### 0.1.1 — 2026-05-31

- First `.app` bundle + DMG artifacts shipping with releases
- Refactor: `build-app.sh` / `build-dmg.sh` accept `--bin-path` so CI can package the cargo-dist-built binary directly

### 0.1.0 — 2026-05-31

First tagged release. cargo-dist pipeline goes live (shell installer, PowerShell installer, MSI, per-arch tar.xz / .pkg).

Day-to-day capabilities prior to v0.1.0: see [`CLAUDE.md`](https://github.com/chris-mclennan/mnml/blob/main/CLAUDE.md) and [`FEATURES.md`](https://github.com/chris-mclennan/mnml/blob/main/FEATURES.md) on the repo.
