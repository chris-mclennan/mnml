# Loop

Fast, keyboard-first team notes. Every note is a Markdown file;
search is `rg`; sync is `git`.

## Setup

```sh
git clone https://bitbucket.org/bloomlabs/loop
cd loop && bun install
bun run dev
```

## Contributing

Small PRs. Green CI. See `docs/CONTRIBUTING.md`.
