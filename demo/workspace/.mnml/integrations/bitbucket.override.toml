# Demo-mode override — see jira.override.toml note. Points at the
# local mock server.
id = "bitbucket"

[chip]
enabled = true
in_palette_bar = false

[env]
BITBUCKET_BASE_URL = "http://localhost:7071/bitbucket"
BITBUCKET_USER = "avachen"
BITBUCKET_ACCESS_TOKEN = "demo-token-notely"
BITBUCKET_APP_PASSWORD = "demo-token-notely"
BITBUCKET_WORKSPACE = "bloomlabs"
BITBUCKET_REPO = "notely"
